#include "client/connexion/controllers/creating_controller.h"

CreatingController::CreatingController(CreatingWindow& creating_window)
    : creating_window(creating_window) {
    (void)this->creating_window;
}
