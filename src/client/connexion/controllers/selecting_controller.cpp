#include "client/connexion/controllers/selecting_controller.h"

SelectingController::SelectingController(SelectingWindow& selecting_window)
    : selecting_window(selecting_window) {
    (void)this->selecting_window;
}
